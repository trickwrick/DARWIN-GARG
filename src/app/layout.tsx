import type { Metadata } from "next";
import { Inter, Playfair_Display } from "next/font/google";
import "./globals.css";

const inter = Inter({
  subsets: ["latin"],
  variable: "--font-inter",
});

const playfair = Playfair_Display({
  subsets: ["latin"],
  variable: "--font-playfair",
});

export const metadata: Metadata = {
  title: "When Gods Must Return | Ancient Wisdom for Modern Chaos",
  description: "The world isn't facing one crisis. It's facing ten — simultaneously. Discover the ancient wisdom of the Dashavatar for our modern chaos.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className={`${inter.variable} ${playfair.variable}`}>
      <body>
        <main>{children}</main>
      </body>
    </html>
  );
}
